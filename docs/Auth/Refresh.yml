info:
  name: Refresh
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/refresh"
  body:
    type: json
    data: |-
      {
        "refresh_token": "{{REFRESH_TOKEN}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.getBody() && res.body.data && res.body.data.access_token) {
          bru.setVar("TOKEN", res.body.data.access_token)
        }
        if (res.getBody() && res.body.data && res.body.data.refresh_token) {
          bru.setVar("REFRESH_TOKEN", res.body.data.refresh_token)
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
