info:
  name: Liveness Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/livez"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
